import 'package:flutter/material.dart';

class AssignTeacherToClassScreen extends StatelessWidget {
  const AssignTeacherToClassScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Assign Teachers'), backgroundColor: Colors.teal, foregroundColor: Colors.white),
      body: const Center(child: Text('Assign teachers feature coming soon')),
    );
  }
}
