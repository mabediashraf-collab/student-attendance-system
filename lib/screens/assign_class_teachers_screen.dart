import 'package:flutter/material.dart';

class AssignClassTeachersScreen extends StatelessWidget {
  const AssignClassTeachersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Assign Class Teachers'), backgroundColor: Colors.teal, foregroundColor: Colors.white),
      body: const Center(child: Text('Assign class teachers feature coming soon')),
    );
  }
}
