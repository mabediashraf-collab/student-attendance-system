import 'package:flutter/material.dart';

class EnrollStudentsScreen extends StatelessWidget {
  const EnrollStudentsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Enroll Students'), backgroundColor: Colors.teal, foregroundColor: Colors.white),
      body: const Center(child: Text('Enroll students feature coming soon')),
    );
  }
}
