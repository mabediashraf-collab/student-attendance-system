import 'package:flutter/material.dart';

class AssignSubjectsScreen extends StatelessWidget {
  const AssignSubjectsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Assign Subjects'), backgroundColor: Colors.orange, foregroundColor: Colors.white),
      body: const Center(child: Text('Assign subjects feature coming soon')),
    );
  }
}
