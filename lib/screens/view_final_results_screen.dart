import 'package:flutter/material.dart';

class ViewFinalResultsScreen extends StatelessWidget {
  const ViewFinalResultsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('View Final Results'), backgroundColor: Colors.purple, foregroundColor: Colors.white),
      body: const Center(child: Text('Results feature coming soon')),
    );
  }
}
